//Faça um algoritmo que leia dois valores inteiros A e B, se os valores de A e B 
//forem iguais, deverá somar os dois valores, caso contrário devera multiplicar A 
//por B. Ao final de qualquer um dos cálculos deve-se atribuir o resultado a uma 
//variável C e imprimir seu valor na tela.

let a = 1
let b = 1

if (a === b)
    c = (`A soma entre A e B é ${a + b}`)
else {
    c = (`A multiplicação entre A e B é ${a * b}`)
}

console.log(c)